# ☕ ⏲ Tea Timer for MacOS 
A small script, that allows you to time your tea on MacOS, since there is no pre-install timer app 😵‍💫 

## 🔧️ Installation
```shell
git clone git@github.com:BeneLuWi/tea-timer.git
cd tea_timer
chmod +x timer.sh
```

Optionally create an alias for your `terminal`:
- Edit your config and add the line `alias timer=/absolute/path/to/repository/timer.sh`

## 🫖 Usage
```
Usage: timer.sh [minutes]
* minutes: Time in minutes, the tea needs
```

## ☕ Features 
- Native Notification
- Native Alert Sound

### 🍪 TODOs 
- [ ] Use a config file for sound and notification text